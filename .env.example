# Database connection (SQLite — zero external dependencies)
DATABASE_URL="file:./dev.db"

# JWT signing secret — change this in any real deployment
JWT_SECRET="dev-secret-change-me-please"

# Token lifetime
JWT_EXPIRES_IN="7d"

# API server port
PORT=4000
